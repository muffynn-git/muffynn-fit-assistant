# LLM_PROVIDER controls whether the optional interpretation layer runs.
# "none" = deterministic engine only, zero external API cost.
LLM_PROVIDER=none

# Only used if LLM_PROVIDER=openai
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Only used if LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=
